# Development environment variables
API_PREFIX=
NODE_ENV=environment
PORT=
BASE_URL=http://IP

# Database
# Estructura: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public
DATABASE_URL="postgresql://USER:PASSWORD@localhost:PORT/DB_NAME?schema=public"
DB_NAME=your_project_name
DB_USER=
DB_PASSWORD=
DB_PORT=

# JWT (Generar strings aleatorios largos para producción)
JWT_ACCESS_SECRET=your_secret_for_access_token
JWT_REFRESH_SECRET=your_secret_for_refresh_token

# Resend (Obtener API Key desde resend.com)
RESEND_API_KEY=re_send_api_key

# App
APP_NAME=your_project_name
APP_EMAIL=example@example.com
FRONTEND_URL=http://IP:PORT

# CORS
ALLOWED_ORIGINS=

# Rate Limiting (opcional - usa valores por defecto si no se especifica)
RATE_LIMIT_WINDOW_MS=
RATE_LIMIT_MAX_REQUESTS=